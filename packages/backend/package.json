{
  "private": true,
  "name": "@taren/backend",
  "scripts": {
    "dev": "cross-env PYTHONUTF8=1 uvicorn main:app --reload --reload-dir=. --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
