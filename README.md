# taren.io

Everything to run a local version:

## Running

To run a local version of the project, in one terminal run:

```sh
pnpm dev
```

And in another terminal run:

```sh
cd packages/backend
uv run arq worker.WorkerSettings
```
