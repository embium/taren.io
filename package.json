{
  "scripts": {
    "dev": "pnpm dev:packages",
    "dev:packages": "pnpm --parallel run dev",
    "build": "pnpm run --recursive --filter=!@taren/backend build"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "npm-run-all": "^4.1.5"
  },
  "packageManager": "pnpm@10.3.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
